# Provision Kubernetes cluster on AWS EC2

This Nebula Workflow provisions a Kubernetes cluster on AWS EC2 instances using the Nebula K8s provisioner step. 

The Workflow is defined in `workflow.yml`.